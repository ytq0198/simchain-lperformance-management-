# 学生成绩信息存证与查询 — 项目设计说明

> 选题：**围绕学生成绩信息存证与查询实现一个成绩管理应用**  
> 技术栈约束：**Hyperledger Fabric** 联盟链 + 链码（智能合约）+ 应用层（SDK）  
> 运行环境：**VMware 虚拟机 / Linux**（Ubuntu 20.04 / 22.04 均可；你当前为 **20.04 desktop**）

本文档在 `deepseek设计思路.txt` 与 `gemini设计思路.txt` 的基础上做取舍：保留**可答辩、可演示、可写进报告**的区块链亮点，避免在 16 学时大作业中过度堆叠运维组件导致无法在虚拟机内按期完成。

---

## 一、设计目标与对照课程要求

| 课程要求关键词 | 本设计中的落点 |
|----------------|----------------|
| 上链 | 成绩写入/更正/作废均以交易形式提交，由 Orderer 排序、Peer 落账 |
| 可追溯 | 使用 Fabric 对同一键的**版本历史**（如 `GetHistoryForKey` 或应用层按 TxID 聚合）展示「录入 → 更正」链路 |
| 不可篡改 | 不在链上物理删除记录；错误成绩采用**作废 + 新记录关联**或**状态字段标记** |
| 多方协作 | 至少两个 MSP（组织）：如 **教务处（权威）** 与 **学院/教师侧（录入）**；可选第三角色 **第三方验真**（单独 MSP 或同链不同证书属性） |
| 存证与查询 | 链上保存成绩资产与关键元数据；**按学生/课程/学期**查询与**验真**接口 |

---

## 二、总体架构（推荐「双组织 + 单通道」为基线）

### 2.1 为何不全盘采用「四组织 + 双通道 + 五个 Orderer」

DeepSeek 方案工程完整度高，但在**单台虚拟机**上容易出现：内存不足、证书与通道配置错误率高、联调周期长。建议：

- **基线版（必做，易验收）**：2 组织、1 通道、1 Orderer（Raft 单节点或 Solo 教学场景）、每组织 1～2 个 Peer、CouchDB 或 LevelDB 二选一。  
- **加分版（选做）**：增加第三个组织（用人单位/第三方）、或增加**私有数据集合（Private Data Collection）**、或增加**简单复核状态机**。

### 2.2 逻辑架构图（文字版）

```
┌─────────────┐     HTTPS/JSON      ┌──────────────────┐
│  Web 前端   │ ◄──────────────────► │  应用后端 (API)   │
│ Vue/React   │                     │ Node.js / Go     │
└─────────────┘                     │  Fabric Gateway   │
                                    └────────┬─────────┘
                                             │ gRPC
                    ┌────────────────────────┼────────────────────────┐
                    ▼                        ▼                        ▼
              ┌──────────┐            ┌──────────┐              ┌──────────┐
              │ Peer Org1│            │ Peer Org2│             │ Orderer  │
              │ (教务处)  │            │ (学院)   │             │          │
              └──────────┘            └──────────┘              └──────────┘
                    │                        │
                    └────────── 同一通道 (如 ScoreChannel) ──────────┘
```

**Gemini 思路采纳点**：双组织联盟链、复合主键/资产模型、**撤销/冲正式更正**体现「不可删除但可治理」。  
**DeepSeek 思路采纳点**：**批量录入**、**审计字段/审计事件**（可先简化为链码内写扩展状态或单独 Audit key）、**一次性授权验真**（应用层实现即可，链上存哈希更专业）。

---

## 三、链上数据模型与智能合约设计

### 3.1 资产（建议键设计）

为便于查询与历史溯源，主键建议采用可读复合键（链码内拼接）：

- **主键示例**：`SCORE | {studentId} | {courseId} | {semester}`  
- **值（JSON）建议字段**：

| 字段 | 说明 |
|------|------|
| `studentId` | 学号（链上可明文，若学校有合规要求可改为仅哈希上链） |
| `courseId` / `courseName` | 课程标识与名称 |
| `semester` | 学期 |
| `score` | 分数（若做私有数据集合，则公开账本仅存 `scoreHash`） |
| `status` | `PENDING`（待教务处审核）/ `ACTIVE` / `REVOKED` / `SUPERSEDED` 等 |
| `prevScoreKey` | 若本条为更正产生，指向被替代记录 |
| `teacherId` / `operator` | 操作者标识（来自客户端身份或参数+链码校验） |
| `updatedAt` | 时间戳 |
| `remark` | 可选备注 |

### 3.2 核心链码接口（Go，基线版）

| 方法 | 作用 | 权限思路 |
|------|------|------------|
| `InitLedger` | 可选：写入演示数据 | 管理员/初始化脚本 |
| `CreateScore` | 首次录入 | 教务处或学院角色（见下节） |
| `UpdateScore` | 更正：旧记录标记 `SUPERSEDED` 或 `REVOKED`，新记录关联 | 教务处背书或 Org1+Org2 背书策略 |
| `RevokeScore` | 作废错误记录（不删除 key 的历史） | 教务处 |
| `QueryScore` | 单条查询 | 学生本人 / 教师本学院 / 教务处（用证书属性或后端过滤） |
| `QueryScoresByStudent` | 按学号列表 | 同上 |
| `GetScoreHistory` | 返回该键的版本历史摘要 | 授权角色 |
| `VerifyScore` | 输入 `studentId,courseId,semester,claimedScore` 返回是否一致 | 可设计为公开查询或需应用层 token |

**仓库实现对照（`score-chaincode`，2026-05-15 起 v2.3）**：链码函数名为 **`PutScore` / `ApproveScore` / `CorrectScore` / `RevokeScore` / `GetScore` / `GetScoreHistory` / `VerifyScore`**，以及申诉相关 **`SubmitAppeal` / `ResolveAppeal` / `ListOpenAppeals` / `ListMyAppeals`**（与上表设计意图对应，非逐字同名）。**`PutScore`** 为 **6 个字符串参数**：学号、课程、学期、分数、备注、**录入方角色**（**`TEACHER`** 或 **`DEAN`**）；**`TEACHER`** 写入时 **`status` 为 `PENDING`**，须由 **`ApproveScore`**（教务处侧）转为 **`ACTIVE`**；**`DEAN`** 可直接 **`ACTIVE`**。**`CorrectScore` / `RevokeScore` / `PutScore`（Org1 写路径）** 在入口调用 **`assertOrg1Mutate`**：**MSP 须为 Org1MSP**；若证书含 Fabric CA 登记的 **`abac.role`** 且为 **`student`** 或 **`verifier`** 则拒绝。申诉提交走 **Org2MSP** 路径（与 **JWT + Gateway 选 Org1/Org2** 组合后，答辩可说明「应用层角色 + 链上 MSP/属性」纵深）。**重要**：拉取含 v2.3 的代码后，须在 VM 上 **`deployCC` 升版**（递增 **`-ccv`/`-ccs`**），否则六参 **`PutScore`** 与新 REST 会与旧链码不兼容。

**说明**：课程大作业中「学生只查本人」若全在链码用 ABAC 做强校验，开发与演示成本较高。推荐折中：

- **链码**：对**写操作**做强 MSP（及可选属性）约束；读操作相对宽松。  
- **应用后端**：**JWT** 绑定业务角色，写接口 **`requireRoles`**；查询侧由应用层控制参数范围（**链下身份 + 链上存证**）。

若追求加分：使用 **Fabric CA** 为不同用户登记 **`abac.role`**，与链码中 **`cid.GetAttributeValue`** 分支联动（cryptogen 默认证书一般无自定义属性，链码仍以 **Org1MSP** 为主判据）。

### 3.3 背书策略（示例）

- **成绩写入/更正**：`AND('OrgEduAdmin.peer', 'OrgCollege.peer')` 或 `OR` 组合按老师要求调整。  
- **查询**：默认通道读请求由背书节点执行即可；若使用私有数据，需配置 collection 并注意 endorser 选择。

### 3.4 私有数据集合（选做加分）

- 账本公开：`scoreHash = SHA256(学号+课程+学期+分数+盐)`  
- 私有集合：存明文分数或敏感字段  
- 第三方验真：由授权方提供明文，链码比对哈希返回 `true/false`（**不直接暴露他人成绩**）

---

## 四、应用层设计

### 4.1 后端（Node.js + Fabric Gateway）

- **连接管理**：按 **Org1 / Org2** 各维护一套 **`Gateway`** 缓存（**`backend/src/fabric.ts`**）。  
- **应用登录与持久化（网站用户）**：
  - **`JWT`**：**`signAuthToken` / `verifyAuthToken`**（**`backend/src/auth.ts`**）。
  - **用户校验与注册**：**`tryLoginUser` / `registerUser`**（**`backend/src/userService.ts`**）。若 **`.env`** 中配置了 **`MYSQL_HOST`**，则使用 **MySQL** 表 **`app_users`**（密码 **bcrypt**）；启动时 **`CREATE DATABASE IF NOT EXISTS`**、建表、空表时种子四条演示账号。若**不配 `MYSQL_HOST`**，则用户仅存**进程内存**，重启丢失。
  - **`POST /api/auth/register`**：演示「CA 叙事」注册；**教务处（Academic_Affairs）** 需 **`inviteCode`**（**`DEMO_REGISTRATION_INVITE`** 或默认 **`CHAIN-EDU-2026`**）；**不调用真实 Fabric CA**。
- **链上身份（与网站用户分离）**：用户经 **`POST /api/auth/login`** 校验后，请求处理时根据 **`JWT` 中的 `role`** 选择 **Org1** 或 **Org2** **磁盘证书**（**`cryptogen` User1**），调用 **`evaluateTransaction` / `submitAsync`**。**新注册网站用户不会自动获得独立 MSP 目录**；答辩需说明生产侧 **Fabric CA enroll** 与用户—证书绑定。
- **鉴权**：除 **`GET /api/health`**、**`POST /api/auth/login`**、**`POST /api/auth/register`** 外，业务接口须有效 **JWT**；**`GET /api/events/stream`** 使用 **`?token=`**。**`requireRoles`** 与链码 **MSP** 写入校验配合（见 **§3.2**）。
- **REST 契约**：以 **`backend/README.md` API 一览** 为准；**`GET /api/health`** 含 **`fabricConfigured`**、**`userStore`（`mysql`|`memory`）**、**`mysqlReachable`**（启用 MySQL 时）。

### 4.2 前端（Vue3）

- **登录页** **`/login`**：动态六边形背景（**`LoginHexBackground`**）；健康状态提示。  
- **注册页** **`/register`**：同风格玻璃拟态 UI；演示 CA 流程动画；教务处需邀请码；成功后助记词弹窗（演示用）。  
- **工作台**：侧栏 + 主内容区；全屏弱动态背景（**`WorkspaceAmbientBackground`**：网格、节点连线、路由切换短暂脉冲），按角色微调色相。  
- **角色与路由**：教务处 / 教师 / 学生 / 用人单位 侧栏与 **`meta.roles` / `requiresWrite`** 一致；**`/forbidden`**。  
- **业务页**：综合看板、链上浏览器、成绩查询/历史/录入/更正/作废/**审核**、申诉/收件箱、验真（含 PDF、二维码、批量验真）、链上身份、**SSE** 订阅（见 **§十二**）。  

**未实现（加分可选）**：**`/scores/import` 批量 CSV**、**`GET /api/chain/summary`** 真实区块高度（Dashboard 部分仍为演示动画 + **`/api/health`**）。

### 4.3 事件监听（选做）

- **本仓库（v2.3）**：后端在 **`submitAsync`** 成功路径上经 **`commitBus`** 向 **`GET /api/events/stream`**（**SSE**）推送「上链成功」类消息；前端 **`useCommitSse`** 订阅。**非** Fabric 原生区块事件直连，便于在单 VM 环境快速演示「异步可感知」。若需加分，可再调研 **链码 `SetEvent` + Gateway 事件** 或 **WebSocket**。

---

## 五、部署与目录结构（建议在仓库中固定）

```
final-project/
├── fabric-network/          # crypto-config, configtx, docker-compose, scripts
├── score-chaincode/         # Go 链码（本仓库实际路径；非下述泛化名的唯一写法）
├── chaincode/score-go/      # 泛化示例名（课程文档常见写法）
├── backend/                 # API 服务
├── frontend/                # Web
├── scripts/                 # 如 start_backend、deployCC 参数备忘等
└── docs/
    ├── 项目设计.md
    └── 实验报告.md
```

- **容器化**：网络与节点用 Fabric 官方示例风格 `docker-compose`；应用层也可容器化，或直接在 VM 装 Node。  
- **一键脚本**：`scripts/network.sh`（启动/停止）、`scripts/deployCC.sh`（打包安装链码）。

---

## 六、测试计划（写进报告「可复现」）

1. **链码单元测试**（可选）：对 JSON 序列化、状态迁移做表驱动测试。  
2. **集成测试脚本**：`curl` 或 `bash` 顺序调用：录入 → 查询 → 更正 → 历史条数增加 → 验真。  
3. **演示脚本**：固定演示账号与数据，避免答辩现场随机失败。

---

## 七、分阶段实施路线（强烈建议按周/按次迭代）

| 阶段 | 产出 | 验收标准 |
|------|------|----------|
| P0 | Fabric 网络起得来，CLI 能 query | `peer` 容器健康，通道创建成功 |
| P1 | 链码安装、Invoke/Query 通 | 能录入并查到一条成绩 |
| P2 | 更正 + 历史溯源 | 历史至少 2 个版本，界面或 API 可见 |
| P3 | 后端 + 前端 | 浏览器完成全流程 | **后端 REST + 宿主机 `frontend/` SPA 已实现**；分工与联调见 **§十四**；答辩前完成 **T7** 截图（**`实验报告.md` §7.2**） |
| P4 | 验真 / 私有数据 / 事件 / 批量导入 | 任选一个作为加分演示 |

---

## 八、风险与虚拟机资源配置

- **内存**：Fabric + CouchDB + 多 Peer 建议 **≥ 4GB**；紧张时减 Peer 数量、用 LevelDB。  
- **与你当前虚拟机对齐**：截图配置为 **内存 4GB、CPU 8 核、磁盘 40GB、NAT**，可跑通教学用网络，但**不要**同时开 Grafana/Prometheus/过多 Peer。建议：**每组织 1 个 Peer**、**Orderer 1 个**、世界状态优先 **LevelDB**；CouchDB 仅在课程强制富查询时再开。  
- **磁盘**：Docker 镜像与日志增长快，预留 **≥ 20GB** 空闲。  
- **时间**：配置文件错误是最大风险；先跑通 `test-network` 再改组织名与通道名，比从零手写 network 更稳。

---

## 九、你后续需要准备或提供的材料

1. **本机/虚拟机**：Ubuntu 版本、CPU 核数、内存（发给我或自己对照第七节调整拓扑）。  
2. **Fabric 版本锁定**：建议选定 **Fabric 2.5.x** 或老师指定版本，全项目统一。  
3. **是否必须前端**：若课程只要求链码 + SDK 脚本，可将前端降级为「最小 HTML」或 Postman 集合。  
4. **学校是否要求国密**（Gemini 提到的 SM2/SM3）：若要求再单独开分支调研 Fabric BCCSP 国密；否则默认国际密码算法即可。  
5. **答辩演示账号**：教务处/教师/学生三类各一个即可。

---

## 十、与两份 AI 思路的对应关系（便于写「方案来源与改进」）

- **来自 DeepSeek 且保留**：批量录入、审计思想、验真 API、分角色控制台、CLI 作为辅助（可用简单 `curl` 脚本代替独立 CLI）。  
- **来自 Gemini 且保留**：双组织模型、撤销/冲正、哈希验真思路、区块高度看板、历史时间轴、`GetHistoryForKey` 溯源叙事。  
- **本文档补充**：**范围裁剪原则**、**链下身份与链上存证分工**、**分阶段里程碑**、与**课程评分点**逐条对齐表，保证在虚拟机与学时约束下可完成。

当你完成代码仓库初版（哪怕只有 P0+P1）后，可把仓库结构或关键报错贴出，我可以再帮你把链码接口与背书策略「收敛到可运行」的具体版本。

---

## 十一、Fabric 要装在哪？必须装在虚拟机里吗？

### 11.1 结论（按大作业验收场景）

| 内容 | 建议安装 / 运行位置 | 原因 |
|------|---------------------|------|
| Docker、Fabric 网络（Peer/Orderer/CA）、链码容器、老师提供的 **Fabric 二进制与镜像**、启动网络的脚本 | **必须在你用于演示的 Ubuntu 虚拟机里** | 作业要求在 Linux VM 上实现；链路与路径按 Linux 设计；答辩时评委通常认「VM 里一键起网 + 浏览器演示」。 |
| 代码编辑（Cursor / VS Code） | **可在 Windows 宿主机** | 仅编辑文件；通过 **共享文件夹、Git、SCP** 与虚拟机同步即可。 |
| 在 Windows 上单独装一套 Fabric 跑完整网络 | **一般不推荐** | WSL2 可跑但环境与课程/老师脚本不一致时排错成本高；除非老师明确要求 Windows 部署。 |

### 11.2 老师发的安装包怎么放

在虚拟机内任选**固定目录**即可，例如：

- `~/fabric/` 下解压：`bin`、`config` 与脚本；在 `~/.bashrc` 里 `export PATH=$PATH:$HOME/fabric/bin`。  
- 或按课程实验手册指定路径（**以老师文档为准**）。

关键是：**PATH 能找到 `peer`、`configtxgen`，Docker 能拉到所需镜像**；与「装在 C 盘还是 D 盘」无关，在 Linux 里就是「装在哪个用户目录」。

### 11.3 小组作业一个人做

在报告「分工说明」里如实写：**本人承担网络、链码、后端、前端与测试**即可；若需交分工表，可写「组员 A：本人全流程；其余组员未参与」以符合诚信要求（具体措辞按学校规定调整）。

---

## 十二、前端界面设计（成绩管理应用）

面向 **4GB 内存虚拟机**：前端只做**静态构建 + 由后端提供 API**，开发时可在 Windows 上 `npm run dev`，**联调与演示**建议在 VM 内或 VM 访问宿主机 IP（NAT 下注意端口转发），避免「仅宿主机可访问 localhost」导致答辩时打不开。

### 12.1 技术选型（二选一即可）

| 方案 | 栈 | 说明 |
|------|-----|------|
| A（推荐上手快） | **Vue 3 + Vite + TypeScript + Element Plus** | 表格/表单多，教务类界面成熟。 |
| B | **React 18 + Vite + TypeScript + Ant Design** | 与方案 A 等价，按个人熟悉度选。 |

HTTP 客户端：**Axios**；路由：**Vue Router / React Router**；状态：页面少可用 **组合式 API / useState**，不必强上 Pinia/Redux。

### 12.2 信息架构与路由

| 路由路径 | 页面名称 | 主要用户 | 功能要点 |
|----------|----------|----------|------------|
| `/login` | 登录 | 全员 | **JWT**；演示账号见 **`操作说明.md`**；**`LoginHexBackground`** |
| `/register` | 注册（演示 CA） | 公开 | **`POST /api/auth/register`**；教务处须 **邀请码**；成功后 **MySQL** 或内存落库 |
| `/forbidden` | 无权访问 | 全员 | 角色或路由策略不满足时展示说明 |
| `/dashboard` | 综合看板 | 全员（侧栏因角色而异） | **`GET /api/health`**；演示级统计与区块流；学生/教务占位卡片 |
| `/explorer` | 链上浏览器 | 教务、教师、用人单位 | 学生无侧栏入口，直链会被重定向 |
| `/scores` | 成绩查询 | 教务、教师、学生 | 单键查询；**双 Org 一致性自检**（**`GET /api/integrity/score-read`**）；成功后可 **PDF**、**验真二维码** |
| `/scores/history` | 全链路溯源 | 教务、教师、学生 | 时间轴、Diff、**TxID**；可选 **tx-insight**（读写集合摘要） |
| `/my-certificates` | 我的证书 | 学生 | 入口与说明聚合 |
| `/scores/new` | 录入上链 | 教务、教师 | **`POST /api/scores`**；上链阶段文案 |
| `/scores/correct` | 成绩更正 | 教务、教师 | **`POST /api/scores/correct`** |
| `/scores/revoke` | 成绩作废 | 教务、教师 | **`POST /api/scores/revoke`** |
| `/verify` | 验真与凭证 | 教务、教师、用人单位 | **`POST /api/verify`**；**用人单位**可见 **批量验真**；支持 **URL 扫码回填** |
| `/scores/approve` | 成绩审核（待激活） | **教务处** | **`POST /api/scores/approve`**；将 **`PENDING`** 批为 **`ACTIVE`** |
| `/appeals` | 学生申诉 | **学生** | **`POST /api/appeals`** |
| `/appeals/inbox` | 申诉处理（待办列表 + 办结） | **教务、教师** | **`GET /api/appeals/open`**、**`POST /api/appeals/resolve`** |
| `/fabric-identity` | 链上身份材料（证书 PEM 摘要） | 全员（侧栏按角色显示） | **`GET /api/auth/fabric-identity`** |
| `/scores/import` | 批量导入 | 教务、教师 | **未实现** |
| `/student` | 我的成绩（独立列表） | 学生 | **未实现**；由 **`/scores`** + **`/my-certificates`** 覆盖主流程 |

> 与旧版「顶栏角色下拉、后端固定 Org1」不同：当前为 **登录后 JWT 决定菜单**，**后端按角色选择 Org1 或 Org2 Gateway**（见 **§4.1**）。

### 12.3 页面布局（统一框架）

- **顶栏**：应用标题「成绩链上存证系统」+ **当前用户显示名 / 角色 / MSP** + **退出登录**。  
- **侧栏**：按 **JWT 角色** 动态生成（学生无写菜单、无链上浏览器与验真直链；**用人单位**收敛到验真、浏览器、**链上身份**等）。
- **内容区**：深色主题卡片承载表格或表单；表格支持加载中与错误提示（Message）。

### 12.4 关键交互与校验

- 提交前前端校验：分数范围（如 0–100）、学号非空、学期格式统一（如 `2024-1`）。  
- 所有写操作展示 **交易 ID** 或后端返回的 `txId`（便于写进实验报告与截图）。**v2.3**：写成功后可经 **SSE**（**`/api/events/stream?token=`**）推送桌面通知类提示（见 **`useCommitSse`**）。  
- 错误展示：网络错误 / 背书失败时显示后端返回的 `message`；**401** 自动回登录，**403** 提示无权。

### 12.5 视觉与报告截图位（无需复杂美工）

- **登录页** **`/login`**、**注册页** **`/register`**：**深色玻璃拟态**；登录为六边形网格背景；注册复用同系组件并强调「身份铸造」叙事。  
- **已登录 shell**：**`App.vue`** 下层 **`WorkspaceAmbientBackground`** —— 低对比移动网格、稀疏节点连线、边缘「区块」微粒；**路由切换**时连线亮度短暂增强；**角色**（教务/教师/学生/核验）微调背景色相。  
- 报告建议截图：**注册成功弹窗**、**工作台背景与侧栏同屏**。

### 12.6 与后端的约定（便于前后端并行）

前端只认 **REST JSON**；**路径、查询参数与请求体字段以仓库 `backend/README.md` 为唯一契约**（下列为早期规划示例，若与 README 不一致，**以 README 为准**）。

- `GET /api/health`（**无需 JWT**；**`fabricConfigured`**、**`userStore`**、**`mysqlReachable`** 等）  
- `POST /api/auth/login`，body：`{ username, password }` → **`token`**、**`user`**、**`fabricProfile`**  
- `POST /api/auth/register`（**无需 JWT**），body：`{ username, password, displayName, role, inviteCode? }` → **201** 与说明文案  
- `GET /api/auth/me`（需 **`Authorization: Bearer`**）  
- `GET /api/scores?...`、`GET /api/scores/history?...`、`GET /api/scores/tx-insight?...`、`GET /api/integrity/score-read?...`、`POST /api/verify`（均需 **Bearer**）  
- `POST /api/scores`、`POST /api/scores/approve`、`POST /api/scores/correct`、`POST /api/scores/revoke`（需 **Bearer**；**`approve`** 仅教务处；其余写入为教务/教师）  
- `POST /api/appeals`（**学生**）、`GET /api/appeals/mine?studentId=`（**学生**）、`GET /api/appeals/open` 与 `POST /api/appeals/resolve`（**教务/教师**）  
- `GET /api/auth/fabric-identity`（需 **Bearer**）  
- `GET /api/events/stream?token=<JWT>`（**SSE**；非 `Authorization` 头）  
- **未在 `backend/` 提供**：`GET /api/chain/summary`、`PUT /api/scores/:key` 等；若报告或界面需要「区块高度 / 最近 tx 列表」，需在 **`backend/`** 增路由后再接前端。

### 12.7 本仓库前端实现状态（与 §12.2 对照）

| 设计路由（§12.2） | 仓库中路径 | 说明 |
|-------------------|------------|------|
| `/login` | **`/login`** | **JWT**；**`LoginHexBackground`** |
| `/register` | **`/register`** | **`apiRegister`**；**`meta.public`** |
| `/forbidden` | **`/forbidden`** | 路由 **`meta.roles` / `requiresWrite`** 与身份不匹配 |
| `/dashboard` | **`/dashboard`** | **`GET /api/health`** + 演示图表/区块流 |
| `/explorer` | **`/explorer`** | 学生被路由层限制 |
| `/scores` | **`/scores`** | 查询 + **PDF** + **验真二维码** + **双读一致性**（integrity） |
| `/scores/history` | **`/scores/history`** | Diff / 时间轴；**tx-insight** 对话框 |
| `/my-certificates` | **`/my-certificates`** | **学生** |
| `/scores/new` | **`/scores/new`** | **`POST /api/scores`** + **`useFabricSubmitFlow`**（含第六参 **TEACHER/DEAN**） |
| 更正 / 作废 | **`/scores/correct`**、**`/scores/revoke`** | 同左 |
| `/verify` | **`/verify`** | **URL query 回填**、**`auto=1`**；**用人单位**批量验真 |
| `/scores/approve` | **`/scores/approve`** | **`POST /api/scores/approve`**；教务处 |
| `/appeals` / `/appeals/inbox` | **`/appeals`**、**`/appeals/inbox`** | 学生提交；教务/教师 **`open` / `resolve`** |
| `/fabric-identity` | **`/fabric-identity`** | **`GET /api/auth/fabric-identity`** |
| **`App.vue` 壳** | **全局** | **`WorkspaceAmbientBackground`**（已登录页）；**`RouterView`** 过渡 |
| **`/scores/import`、`/student`、动态 `:key` 详情页** | — | **未实现** |

**开发与构建**：宿主机 **`cd frontend && npm install && npm run dev`**；提交前 **`npm run build`**。详见 **`frontend/README.md`**、**`操作说明.md` §四**；**日常先起 Fabric 再起后端再起前端的顺序**见 **`操作说明.md` §〇** 与 **§十四 14.3**。

**下一步（可选）**：**`GET /api/chain/summary`**、**`/scores/import`** 等与 §12.2 未实现项对齐。

---

## 十三、企业化增强（结合 DeepSeek / Gemini，按你当前虚拟机可落地）

> **现状（结项，2026-05-15）**：Ubuntu 20.04、约 **4GB RAM**、**test-network**（Org1/Org2 + 单 Orderer + LevelDB）、**Fabric 2.4.6**；链码 **`score`** 已覆盖 **录入/更正/作废/历史/验真**，**v2.3** 起增加 **`PENDING`/`ApproveScore`**、**申诉**、**`PutScore` 六参**（须在 VM **`deployCC` 升版** 后链上生效）。应用层：**JWT**、**双 Org Gateway**、**MySQL 可选（`app_users`、注册、`bcrypt`）**、**SSE**、**integrity / tx-insight**；前端：**`/login` / `/register`**、**`WorkspaceAmbientBackground`**（见 **§4、§12**）。  
> **原则**：企业化主要体现在 **治理模型（状态机）、可审计、API 契约、身份与职责分离、可复现部署与验收**；**不**在单台作业机上复制 DeepSeek 的「四组织 + 双通道 + 五 Orderer + Prometheus/Grafana + ELK」。

### 13.1 从两份思路里「该拿」与「该扔」

| 来源 | 建议保留（写入报告可讲故事） | 建议暂不实施（与 4GB / 学时冲突） |
|------|------------------------------|-----------------------------------|
| **DeepSeek** | 批量录入（**后端解析 CSV**，链码循环或分批提案）、**审计字段/操作者/时间戳**、验真 API、**分角色控制台**、一键脚本思想（收敛为 `scripts/*.sh` + README） | 四组织、双通道、五 Raft Orderer、每组织双 Peer、独立审计通道、Kong、RabbitMQ、Prometheus+Grafana 常驻 |
| **Gemini** | **冲正/作废（逻辑删除）**、**`GetHistoryForKey` 溯源**、简易 **Dashboard（区块高度 + 最近 txId）**、可选 **哈希验真**（链上存 `sha256`，验真时比对） | 国密 BCCSP（除非课程硬性要求；否则调研成本高） |
| **本仓库 v2.3** | **审核流（`PENDING`）**、**申诉**、**双读一致性演示**、**SSE 上链通知**、**证书 PEM 摘要** | 仍非完整监控栈；**`chain/summary`** 与 **CSV 导入** 仍为可选 |
| **二者** | **私有数据集合（PDC）**：仅当报告需要「隐私加分」且你愿意改 lifecycle 部署时再上；否则用 **哈希 + 应用层授权** 已够答辩 | 在链码里做复杂统计/异常检测（占内存、难测） |

### 13.2 「企业化」最小闭环（推荐你作为项目目标）

1. **链上资产生命周期**：**`PENDING`（教师录入待审）→ `ACTIVE`**（教务处 **`ApproveScore`**）；以及 **`ACTIVE` →（更正）`SUPERSEDED` 或 `REVOKED` + 新键/新记录关联 `prevScoreKey`**（与 §3.1 表一致）；**申诉**为独立复合键状态机（链码 **`APPEAL_*`**）。  
2. **链上审计最小实现**：每条 `ScoreRecord` 带 `operator`、`updatedAt`、`status`；写操作可选 `SetEvent` 发合约事件（后端订阅选做）。  
3. **链上只信多背书**：沿用 test-network 默认 **AND(Org1, Org2)** 于写操作；查询可走单 Peer。  
4. **应用层企业惯例**：Node 后端 **单例 Gateway**、`.env` 管理连接与钱包路径、**结构化日志**（请求 id + txId）、**REST 错误体**统一 `{ code, message, detail? }`。  
5. **验收物**：`scripts/demo.sh`（curl 顺序：录入 → 查询 → 更正 → 历史 ≥2 条 → 验真）、`实验报告.md` 中贴 **txId** 与截图。

### 13.3 链码演进顺序（仍在 `score-chaincode`；升级时使用 **`deployCC -ccv x.y -ccs` 递增 Sequence**）

| 顺序 | 能力 | 说明 |
|------|------|------|
| 1 | **数据模型对齐 §3.1** | 在现有 JSON 上增加 `status`、`updatedAt`、`operator`（字符串即可）、可选 `remark` |
| 2 | **`CorrectScore` / `RevokeScore`** | 旧记录改状态 + 新记录或仅作废；**禁止物理删除** |
| 3 | **`GetScoreHistory`** | 封装 `GetHistoryForKey`，返回可 JSON 化的版本列表（txId、时间、值摘要） |
| 4 | **`VerifyScore`** | 入参「声称分数」与链上当前 `ACTIVE` 记录比对；可选增加 `VerifyScoreHash` |
| 5 | （已实现）**`assertOrg1Mutate`（写入 ABAC）** | **`PutScore` / `CorrectScore` / `RevokeScore`** 等 Org1 写路径：**Org1MSP** + 可选 **`abac.role`** 拒绝 **`student`/`verifier`**；**须 `deployCC` 升版** 后链上生效 |
| 6 | （已实现，v2.3）**审核与申诉** | **`PutScore` 六参**（**`TEACHER`→`PENDING`**、**`DEAN`→`ACTIVE`**）、**`ApproveScore`**；**`SubmitAppeal` / `ResolveAppeal` / `ListOpenAppeals` / `ListMyAppeals`**；**须 VM 再 `deployCC` 升版**（如 **1.2**）与后端/前端同步 |
| 7 | （可选）**`cid` 读 MSP/OU** | 与第 5 步互补；答辩说明「生产可换 Fabric CA 属性」 |

每步完成后在 VM 上：**`go mod tidy` → `vendor` → `deployCC` 升版**，并用 **`peer` + Args** 或后端脚本留证。

### 13.4 应用层顺序（`~/work/score-app` 或仓库 `backend/`）

| 顺序 | 产出 |
|------|------|
| B1 | **Express** + **`GET /api/health`**、**`POST /api/auth/login`**、**`GET /api/auth/me`**（JWT）；**`GET /api/chain/summary`**（summary 可先占位，**未实现**） |
| B2 | 接入 **Fabric Gateway**：按 JWT 角色选 **Org1/Org2**；`POST/GET` 成绩与 **txId** 回传；业务路由 **鉴权** |
| B3 | **CSV 批量**：仅后端读文件，循环提交交易（失败行返回列表） |
| B4 | 前端 **Vue/React** 按 §12 路由做「角色切换 + 列表 + 详情 + 历史 + 验真」——**基线已落地**；**v2.3** 已加 **审核、申诉、tx-insight、integrity、fabric-identity、SSE**（见 **§12.7**）；**详情动态路由**、**批量导入**、**完整 `chain/summary`** 仍为可选 |
| B5 | （已实现，v2.3）**REST 扩展 + SSE** | **`/api/scores/approve`**、**`/api/appeals*`**、**`/api/scores/tx-insight`**、**`/api/integrity/score-read`**、**`/api/auth/fabric-identity`**、**`/api/events/stream`**；进程内 **`commitBus`** |

开发机内存紧张时：**VM 只跑 Docker Fabric**；Node 可在 Windows 跑但演示前须在 VM 或 NAT 下验证浏览器能访问。

### 13.5 你现在「具体该怎么干」（按周也可压缩）

1. **冻结拓扑**：继续 **test-network + mychannel + LevelDB**，不扩 Peer、不加 CouchDB（除非老师要求富查询）。  
2. **改链码（优先）**：实现 §13.3 的 1→3（模型 + 更正/作废 + 历史）；CLI 或临时 `node` 脚本调通后再写完整后端。  
3. **升版部署**：每次改链码 → `deployCC -ccn score -ccv x.y -ccs 递增`，报告里记录 **Package ID / Sequence**。  
4. **起后端**：最小 REST + Gateway，把 **`peer` 里已验证的 Args 调用** 搬进 `submitTransaction` / `evaluateTransaction`。  
5. **起前端**：仓库 **`frontend/`** 已覆盖 **Dashboard、查询、历史、录入、更正、作废、验真**、**审核、申诉、身份页** 与 **SSE**；答辩前配置 **`VITE_API_BASE`** 并补截图（见 **`实验报告.md` §6.4、§7.2**）。可选迭代：**`/scores/import`**、**`/api/chain/summary`**。  
6. **写报告**：对照 §13.2「企业化最小闭环」逐条写「实现了什么、截图在哪、与中心化方案对比」。

### 13.6 答辩一句话（建议背下来）

「我们在 **双组织单通道** 上模拟 **教务处与学院** 共同背书写入；链上成绩有 **状态与历史版本**（含 **`PENDING` 审核** 与 **申诉**），更正不做物理删除；应用层负责 **身份与界面**，链上负责 **存证与可审计**；监控与多 Orderer 属于生产扩展，本实验聚焦 **共识与背书下的业务正确性**。」

---

## 十四、宿主机与虚拟机多线推进（P3 起）

从 **P3（后端 + 前端）** 开始，建议**两条线并行**：**Windows 宿主机**负责编辑与界面迭代；**Ubuntu 虚拟机**负责 Fabric、链码容器与「与真实账本」的最终联调。这样既不违背「网络在 Linux VM 上跑」的验收习惯，又能减少在 VM 里开 IDE 的卡顿。

### 14.1 分工一览

| 工作项 | 建议位置 | 说明 |
|--------|----------|------|
| 代码编辑（链码、**`backend/`**、**`frontend/`**、文档） | **宿主机** | Cursor / VS Code；路径与换行按仓库规范即可。 |
| **Docker、test-network、Peer/Orderer、链码部署与 `peer` CLI** | **虚拟机** | 与课程要求、答辩演示一致。 |
| **后端进程（Node + Fabric Gateway）** | **优先虚拟机**（与链同机） | **`FABRIC_CRYPTO_BASE`** 指向 VM 内 **`…/org1.example.com`** 绝对路径，避免 Windows 路径与证书挂载错位。 |
| **前端静态页 / SPA 开发** | **宿主机** | 开发时将 API 基地址设为 **`http://<虚拟机 NAT IP>:<后端端口>`**（默认后端 **`3000`** 时即 **`http://<VM_IP>:3000`**）；浏览器在宿主机访问即可（NAT 端口转发由 VMware/VirtualBox 配置）。 |
| **答辩前整体验收** | **虚拟机内浏览器或宿主机指 VM IP** | 以「能连上 VM 上起的后端 + 账本」为准留截图。 |

### 14.2 同步方式（避免「拖文件夹丢权限」）

1. **首选 Git**：宿主机 `commit` / `push`，虚拟机 `clone` 或 `pull` 到 **`~/work/`** 下各子目录（或 **`~/work/final-project`** 整仓），再按 **`score-chaincode/README-VM.md`** 做 **`chmod`** / **`vendor`**。**`fabric-packages`** 从宿主机拷贝时须保留 **`organizations/cryptogen`**。  
2. **共享文件夹拖拽**：每次拖入后执行一次 **`chmod -R u+w`**（或文档中的 **`fixwork`**），否则 **`go mod vendor`** 可能报权限错误。  
3. **链码与网络**：改链码后仍在 **VM** 上 **`deployCC` 升版**；宿主机只改源码，不在 Windows 上跑完整 Fabric 网络（除非老师明确要求）。

### 14.3 联调顺序（建议；与 `操作说明.md` §〇 一致）

以下**不要求**修改业务源码；链码与通道已就绪、当日仅做演示时，可跳过 **`deployCC`** 与 **`createChannel`**（以 **`docker ps`**、**`querycommitted`** 为准）。

1. **VM：Fabric 网络** — **`./network.sh up`**；必要时 **`createChannel`**；若尚未部署 **`score`** 或需升版，再 **`deployCC`**（定义与版本见实验报告 **§6.5 / §7.1**）。**`docker ps`** 确认 **orderer、各 org peer** 为 **Up**。  
2. **VM：后端配置** — **`cd ~/work/backend`**；**`cp .env.example .env`**（若尚无）；**`nano .env`**：**`FABRIC_CRYPTO_BASE`**、**`JWT_SECRET`**；**可选 `MYSQL_*`**（启用 **`userStore":"mysql"`** 与用户持久化，见 **`backend/README.md`**）。  
3. **VM：编译与启动** — **`npm install`** → **`npm run build`** → **`npm start`**（须在 **`backend` 目录内**）。  
4. **VM：自检** — **`GET /api/health`**：查看 **`fabricConfigured`**、**`userStore`**、**`mysqlReachable`**；**`POST /api/auth/login`** 须返回 **`token`**。链码未 **commit** 时执行 **`deployCC`**（**`操作说明.md` §二**）。  
5. **背书失败时** — 若 **`submitAsync`** 报背书相关错误：对照 **`peer chaincode invoke`** 的双 Peer **TLS** 参数与 **test-network** 默认背书策略（在 **`backend/README.md`** 或实验报告 **§9** 留结论）。  
6. **宿主机：前端** — **`frontend/.env.development`** 中 **`VITE_API_BASE=http://<VM_IP>:3000`**（**不要**写 **`localhost:3000`** 除非后端也在宿主机）；**`npm install`**（按需）→ **`npm run dev`**；浏览器打开 Vite 给出的本地地址（多为 **5173**）。跑通后截图写入 **实验报告 §6.4 / §7.2**。

**日常答辩/演示**：只重复 **步骤 1（若已 Up 则跳过）→ 3 → 4 → 6** 即可，详见 **`操作说明.md` §〇**。

### 14.4 可选：宿主机无账本时的 Mock

若宿主机暂时连不上 VM，可在 **`backend/`** 增加环境变量（如 **`USE_MOCK_FABRIC=true`**）走内存假数据，仅用于 UI 与接口契约开发；**链上行为与 T1～T5 证据仍以 VM + CLI 或真实 Gateway 为准**，报告里需区分「Mock 开发」与「真实联调」截图。
